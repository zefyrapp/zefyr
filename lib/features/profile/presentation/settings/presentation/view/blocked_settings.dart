import 'package:flutter/material.dart';

class BlockedSettings extends StatelessWidget {
  const BlockedSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

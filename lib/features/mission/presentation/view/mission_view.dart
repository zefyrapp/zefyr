import 'package:flutter/material.dart';

/// Экран с миссиями
class MissionView extends StatelessWidget {
  const MissionView({super.key});

  @override
  Widget build(BuildContext context) => Column(children: [Container()]);
}
